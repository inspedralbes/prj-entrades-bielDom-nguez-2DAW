# Base de dades (monorepo)

- **`init.sql`**: comentaris d’ajuda; l’esquema real ve de **`php artisan migrate`** a `backend-api/`.
- Migracions i model: [data-model.md](../specs/001-seat-map-entry-validation/data-model.md).
- **Postgres + PostGIS**: `docker/dev/docker-compose.yml`.
